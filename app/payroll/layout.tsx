// PATH: app/payroll/layout.tsx
// This re-exports the admin layout so the Payroll page gets
// the same sidebar, background, and spacing as all admin pages.
export { default } from '@/app/admin/layout';